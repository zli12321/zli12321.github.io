---
layout: default
---
<div class="post">
  {% if page.social %}
    <div class="social social-top">
      <div class="contact-icons">{% social_links %}</div>
      <div class="contact-email">email: zli12321 [at] umd.edu</div>
    </div>
  {% endif %}

  <header class="post-header post-header-compact">
    {% if page.profile and page.profile.image %}
      {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
      <div class="profile-avatar">
        {%
          include figure.liquid loading="eager" path=profile_image_path class="img-fluid rounded-circle" alt=page.profile.image
          cache_bust=true
        %}
      </div>
    {% endif %}
    <div class="header-text">
      <h1 class="post-title">
        {% if site.title == 'blank' %}
          <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
          {{ site.last_name }}
        {% else %}
          {{ site.title }}
        {% endif %}
      </h1>
      <p class="desc">{{ page.subtitle }}</p>
    </div>
  </header>

  <article>
    <div class="clearfix">{{ content }}</div>

    <!-- News -->
    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">news</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Latest posts -->
    {% if page.latest_posts and page.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">latest posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">selected publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% if site.plugins contains 'al_newsletter' %}
        {% include plugins/al_newsletter_form_center_margin.liquid %}
      {% else %}
        {% include newsletter.liquid center=true %}
      {% endif %}
    {% endif %}
  </article>
</div>

<style>
  .post .social.social-top {
    text-align: left;
    margin-bottom: 1.2rem;
  }
  .post .social.social-top .contact-icons {
    font-size: 1.8rem;
  }
  .post .social.social-top .contact-icons a {
    padding: 0 0.35rem;
  }
  .post .social.social-top .contact-icons a:first-child {
    padding-left: 0;
  }
  .post .social.social-top .contact-icons a img {
    width: 1.5rem;
    height: 1.5rem;
    margin-bottom: 0;
  }
  .post .social.social-top .contact-icons a svg {
    width: 1.6rem;
    height: 1.8rem;
  }
  .post .social.social-top .contact-email {
    text-align: left;
    font-size: 0.95rem;
    margin-top: 0.4rem;
    color: var(--global-text-color);
  }
  .post-header-compact {
    display: flex;
    align-items: center;
    gap: 1.1rem;
    margin-bottom: 1.2rem;
  }
  .post-header-compact .profile-avatar {
    flex: 0 0 auto;
  }
  .post-header-compact .profile-avatar figure,
  .post-header-compact .profile-avatar picture {
    margin: 0;
    display: block;
  }
  .post-header-compact .profile-avatar img {
    width: 84px;
    height: 84px;
    object-fit: cover;
    border-radius: 50%;
  }
  .post-header-compact .header-text {
    min-width: 0;
  }
  .post-header-compact .post-title {
    font-size: 2rem;
    margin-bottom: 0.2rem;
    line-height: 1.15;
  }
  .post-header-compact .desc {
    margin-bottom: 0;
    font-size: 0.95rem;
  }
</style>
